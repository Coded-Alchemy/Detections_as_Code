# ============================================
# GitHub Actions build file
# ============================================
# This file describes the CI/CD automation steps.

name: Build and Test Detections
on:
  push:
    branches: ["main", "dev"]
  pull_request:
    branches: ["main", "dev"]

jobs:
  build:
    runs-on: [self-hosted, linux, x64]

    env:
      SPLUNK_USERNAME: ${{ secrets.SPLUNK_USERNAME }}
      SPLUNK_PASSWORD: ${{ secrets.SPLUNK_PASSWORD }}
      SPLUNK_URL: ${{ secrets.SPLUNK_URL }}

    # Timeout to prevent hanging jobs
    timeout-minutes: 15
    
    steps:
    # Project Checkout
    - name: Checkout
      uses: actions/checkout@v4

    # Clean workspace before starting
    - name: Clean Workspace
      run: |
        rm -rf generated/
        rm -f terraform/locals.tf

    # Install dependencies
    - name: Install Dependencies
      run: |
        python3 -m pip install --upgrade pip pyyaml

    # Generate locals.tf automatically
    - name: Generate Terraform Locals from Sigma Rules
      run: |
        chmod +x scripts/generate_locals.py
        python3 scripts/generate_locals.py
        
        echo "### Generated Terraform Configuration" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`hcl" >> $GITHUB_STEP_SUMMARY
        head -n 50 terraform/locals.tf >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

    # Convert Sigma rules into SPL
    - name: Convert Sigma to SPL
      run: | 
        set -e
        mkdir -p generated/splunk
        
        echo "Converting Sigma rules to SPL..."
        rule_count=0
        
        while IFS= read -r rule_file; do
          file_name=$(basename "$rule_file" .yml)
          
          if sigma convert --target splunk --pipeline splunk_windows "$rule_file" > "generated/splunk/${file_name}.spl" 2>&1; then
            if [ -s "generated/splunk/${file_name}.spl" ]; then
              echo "${file_name}.spl"
              rule_count=$((rule_count + 1))
            else
              echo "${file_name}.spl is empty"
              exit 1
            fi
          else
            echo "Failed to convert $rule_file"
            exit 1
          fi
        done < <(find sigma_rules -type f -name "*.yml")
        
        if [ "$rule_count" -eq 0 ]; then
          echo "ERROR: No rules converted"
          exit 1
        fi
        
        echo "Converted $rule_count rule(s)"
        ls -lh generated/splunk/

    # Save SPL artifact
    - name: Upload SPL as an artifact
      uses: actions/upload-artifact@v4.3.1
      with:
        name: spl-detections
        path: generated/splunk
        if-no-files-found: warn
        compression-level: 6

    # Terraform formatting
    - name: Terraform Format
      working-directory: terraform
      run: terraform fmt -recursive

    # Initialize Terraform
    - name: Terraform Init
      working-directory: terraform
      env:
        TF_VAR_splunk_url: ${{ secrets.SPLUNK_URL }}
        TF_VAR_splunk_username: ${{ secrets.SPLUNK_USERNAME }}
        TF_VAR_splunk_password: ${{ secrets.SPLUNK_PASSWORD }}
        TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
        TF_VAR_insecure_skip_verify: "true"
      run: terraform init -reconfigure

    # Destroy existing resources
    - name: Destroy Existing Resources
      working-directory: terraform
      env:
        TF_VAR_splunk_url: ${{ secrets.SPLUNK_URL }}
        TF_VAR_splunk_username: ${{ secrets.SPLUNK_USERNAME }}
        TF_VAR_splunk_password: ${{ secrets.SPLUNK_PASSWORD }}
        TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
        TF_VAR_insecure_skip_verify: "true"
      run: |
        echo "Destroying all existing Terraform-managed resources..."
        terraform destroy -auto-approve || echo "Nothing to destroy"
      continue-on-error: true

    # Clean state completely
    - name: Clean Terraform State
      working-directory: terraform
      run: |
        echo "Removing all state..."
        rm -f terraform.tfstate*
        rm -rf .terraform/
        terraform init -reconfigure

    # Test Terraform before deployment
    - name: Terraform Validate
      working-directory: terraform
      env:
        TF_VAR_splunk_url: ${{ secrets.SPLUNK_URL }}
        TF_VAR_splunk_username: ${{ secrets.SPLUNK_USERNAME }}
        TF_VAR_splunk_password: ${{ secrets.SPLUNK_PASSWORD }}
        TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
        TF_VAR_insecure_skip_verify: "true"
      run: terraform validate

    # Terraform plan
    - name: Terraform Plan
      id: plan
      working-directory: terraform
      env:
        TF_VAR_splunk_url: ${{ secrets.SPLUNK_URL }}
        TF_VAR_splunk_username: ${{ secrets.SPLUNK_USERNAME }}
        TF_VAR_splunk_password: ${{ secrets.SPLUNK_PASSWORD }}
        TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
        TF_VAR_insecure_skip_verify: "true"
      run: |
        terraform plan -out=tfplan
        terraform show tfplan

    # Terraform apply
    - name: Terraform Apply
      working-directory: terraform
      env:
        TF_VAR_splunk_url: ${{ secrets.SPLUNK_URL }}
        TF_VAR_splunk_username: ${{ secrets.SPLUNK_USERNAME }}
        TF_VAR_splunk_password: ${{ secrets.SPLUNK_PASSWORD }}
        TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
        TF_VAR_insecure_skip_verify: "true"
      run: |
        terraform apply -auto-approve tfplan
        
        echo "### Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Deployed Detection Rules:**" >> $GITHUB_STEP_SUMMARY
        terraform output -json deployed_detections 2>/dev/null | jq -r 'to_entries[] | "- **\(.value.name)** (Severity: \(.value.severity))"' >> $GITHUB_STEP_SUMMARY || echo "- Could not generate summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Total Rules Deployed:** $(terraform output -raw detection_count 2>/dev/null || echo 'unknown')" >> $GITHUB_STEP_SUMMARY

    # Verify deployment in Splunk
    - name: Verify Saved Searches in Splunk
      env:
        SPLUNK_USERNAME: ${{ secrets.SPLUNK_USERNAME }}
        SPLUNK_PASSWORD: ${{ secrets.SPLUNK_PASSWORD }}
        SPLUNK_URL: ${{ secrets.SPLUNK_URL }}
      run: |
        echo "=== Verifying Saved Searches in Splunk ==="
        
        SEARCHES=$(curl -k -s -u "$SPLUNK_USERNAME:$SPLUNK_PASSWORD" \
          "$SPLUNK_URL/services/saved/searches?output_mode=json&count=0" \
          | jq -r '.entry[] | select(.content.is_scheduled == "1") | .name')
        
        if [ -z "$SEARCHES" ]; then
          echo "❌ NO SAVED SEARCHES FOUND!"
          exit 1
        else
          echo "✅ Found saved searches:"
          echo "$SEARCHES"
          
          SEARCH_COUNT=$(echo "$SEARCHES" | wc -l)
          echo ""
          echo "Total: $SEARCH_COUNT scheduled searches"
        fi

    # Clean up
    - name: Cleanup
      if: always()
      run: |
        echo "Cleaning up temporary files..."
        rm -rf terraform/.terraform/
        rm -f terraform/tfplan
        rm -f terraform/terraform.tfstate.backup